
### Cannot set property 'onclick' of null 问题
运行JS代码时遇到一个错误，本来以为是字母拼写错误，后来证明是因为结点需要加载完成后才能执行
相应的事件。因为JS文件是引用外部文件，所以需要将JS文件的引用移出head标签外，放置在body标签的
最下面即可。

###JS中文出现乱码的问题

###JS判断对象是否为空的方法
JS中定义的一个对象，但是没有赋值，怎么判断其是否为空呢？
例如：var aqiData = {};
可以使用for(var i in aqiData){} 对aqiData对象进行遍历后判断i是否有效，如果有效表明不为空。

###JS delete实现对象对象属性的删除
delete只能实现删除对象的属性，但是优势delete可以删除对象，这是由以下几种规则产生：
* 变量和函数的声明实际上都会成为全局对象或者当前函数活动对象的属性。
* 属性都有一个DontDelete标记，用于表明该属性是否能被delete。
* 变量和函数的声明创建的属性都会带有DontDelete标记。
* 函数内建的arguments对象作为该函数活动对象的默认属性，创建时总会带有DontDelete标记。
* 在eval代码块中声明的变量和方法都不带有DontDelete标记。
* 对还不存在的变量或属性的直接赋值产生的对象不会带有任何标记，包括DontDelete。
* 对于宿主对象而言，delete操作的结果有可能是不可预料的。

###JS 给动态生成的元素添加事件
最好是在动态生成元素的时候就将事件绑定在上面，这样会比较方便。