
###原生的强大DOM选择器querySelectAll()
接收的参数为CSS选择符，返回的是所有匹配的元素而不仅仅是一个元素，是一个nodelist的实例。
querySelect()返回的是匹配的第一个元素。

###textContent属
用于选择元素含有的所有文本内容，包含空格，可以用于后续文本内容的分割

###sort重排序的方法
默认情况下，sort按照升序排列，会调用每个数组项的toString()方法，然后比较得到的字符串。

不过，你也可以在sort中输入一个比较函数的参数，以便能够确定哪个值位于哪个值前面。